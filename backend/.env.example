# ================================================================
# CONFIGURACION DE BASE DE DATOS - PROYECTO FULLSTACK
# ================================================================

# Configuracion de PostgreSQL
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=proyecto_fullstack
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# Configuracion del servidor
PORT=4000
NODE_ENV=development

# Configuracion de JWT
JWT_SECRET=tu_jwt_secret_muy_largo_y_seguro_aqui_debe_ser_aleatorio
JWT_EXPIRATION=8h

# Configuracion de CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001

# Configuracion de logging
LOG_LEVEL=info

# ================================================================
# INSTRUCCIONES:
# 1. Copia este archivo como .env en la carpeta backend/
# 2. Cambia 'tu_password_aqui' por tu password real de PostgreSQL
# 3. Cambia 'tu_jwt_secret...' por un string aleatorio largo
# 4. Guarda el archivo y reinicia el servidor
# ================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=proyecto_fullstack
DB_USER=postgres
DB_PASSWORD=tu_contrasena_postgresql_aqui

# Configuracion del servidor
PORT=3001
NODE_ENV=development

# Configuracion de JWT (para autenticacion)
JWT_SECRET=tu_clave_secreta_jwt_muy_segura_aqui
JWT_EXPIRES_IN=24h

# Configuracion de CORS
FRONTEND_URL=http://localhost:3000

# Configuracion de logs
LOG_LEVEL=info

# ================================================================
# INSTRUCCIONES:
# 1. Copiar este archivo como .env en la carpeta backend/
# 2. Cambiar DB_PASSWORD por tu contrasena real de PostgreSQL
# 3. Cambiar JWT_SECRET por una clave secreta segura
# ================================================================
